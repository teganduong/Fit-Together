// TODO: Top Nav bar. May want to DELETE if not needed
// Currently used in views
import React, { Component } from 'react';

const NavBar = () => (
  <ul className="top-nav">
    <a><button className="toggleSidebar"></button></a> 
  </ul>
);

export default NavBar;
